import React from "react";
import { Container, Typography } from "@mui/material";
import MainLayout from "layouts/MainLayout";

const Home = () => (
  <MainLayout>
    <Container>
      <Typography variant="h3">This is home screen</Typography>
    </Container>
  </MainLayout>
);

export default Home;
